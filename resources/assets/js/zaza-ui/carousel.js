import Flickity from 'flickity';

const redils = new Flickity('#carousel', { // eslint-disable-line no-unused-vars
    autoPlay: true,
    prevNextButtons: false,
    pageDots: false,
    wrapAround: true,
    autoPlay: true,
    autoPlay: 3500
});
